DATABASE_URL="file:./prisma/dev.db"
PORT=3001

# WARNING: JWT_SECRET must be changed to a long, random value before any
# non-local deployment. The default below is intentionally weak and must
# never be used in staging or production.
# Generate a strong secret: openssl rand -hex 64
JWT_SECRET=changeme-replace-in-production
